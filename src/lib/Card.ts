class Card {
  rank: string;
  suit: string;
  symbol: string;

  constructor(rank: string, suit: string, symbol: string) {
    this.rank = rank;
    this.suit = suit;
    this.symbol = symbol;
  }
}
export default Card;